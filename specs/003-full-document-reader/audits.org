#+TITLE: W003 - Full document reader audit evidence
#+STARTUP: content
#+OPTIONS: toc:2 num:nil

* Definition gate

- Artefact :: [[file:spec.org][W003 - Full document reader for macOS and iOS]].

** Local review - round 1
:PROPERTIES:
:ROUND: 1
:DATE: 2026-09-07
:END:

- Revision :: SHA-256
  =afc5aa911b10c06eb7c1515a80d378321d4e0f45b600c0d25aa3ab8d2daa248f=.
- Context :: Authoring context; not independent evidence.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions= applied
  together under the definition gate.
- Verdict :: PASS.
- Findings and remediation :: Local review clarified dismissal during pending
  rendering, added explicit source-hash timing after intentional fixture edits,
  and traced startup document-access review to the lifecycle criterion. The
  reviewed revision contains these corrections.
- Specification review :: Eight described criteria have reciprocal test links;
  the opening summary matches the scope, design, platform and validation
  boundaries. Historical requirements are explicitly superseded, with
  simplification choices exposed for operator sign-off.
- Design review :: Public host rendering and a modal keep the implementation
  small. The session helper isolates cancellation and stale asynchronous work.
  Native full-document discovery remains an explicit delivery risk, not a
  documentation-derived success claim.
- Test-definition review :: One retained test protects owned state transitions;
  two real-platform user tests cover the central integration, and a bounded
  source/security review covers non-functional boundaries. No simulated native
  reader or permanent dependency-wrapper test is required.
- Checks :: Pandoc parsed the Org hierarchy and links; sanitizer produced no
  differences; =git diff --check= reported no whitespace errors. Product tests
  were not executed during definition.

** External review - round 1
:PROPERTIES:
:ROUND: 1
:DATE: 2026-09-07
:CONTEXT: /root/definition_audit
:END:

- Revision :: SHA-256
  =afc5aa911b10c06eb7c1515a80d378321d4e0f45b600c0d25aa3ab8d2daa248f=.
- Auditor :: Codex, OpenAI =gpt-5.6-luna=, one retained context; timeout 8 minutes.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions= together.
- Verdict :: FAIL.
- Finding 1 :: [audit-test-definitions] [BLOCKING] The stated build, package,
  manifest and installation contract lacked a criterion and observable
  artefact/destination verification.
- Finding 2 :: [audit-design] [BLOCKING] Installation did not specify its
  destination interface, invalid-target behaviour, replacement semantics or
  path boundary.
- Finding 3 :: [audit-spec] [ADVISORY] Notice and documentation brevity needed
  required message content or explicit human wording review.
- Remediation :: Added [[file:spec.org::#ac-003-9][AC003.9 - Installable package and explicit destination]]
  and [[file:spec.org::#ot-003-2][OT003.2 - Build and local installation]]. Defined
  =PLUGIN_DIR=, fixed-file copying, preservation, invalid targets, explicit
  partial-copy failure and the existing operator-managed symlink route.
  Defined notice content and left exact phrasing to human review. The reader's
  product behaviour was not expanded.
- Supersedes :: None; first independent review. Revised-candidate review is pending.

** Local review - round 2
:PROPERTIES:
:ROUND: 2
:DATE: 2026-09-07
:END:

- Revision :: SHA-256
  =9a54776bb07f54cf5163f46600d6d75640286bcb582c5a695a82a571c89e71c5=.
- Context :: Authoring context; not independent evidence.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions= applied
  together to the remediation and adjacent scope.
- Verdict :: PASS.
- Remediation :: The package criterion and bounded installation check cover
  the missing build and destination contract. Destination selection, fixed-file
  replacement, unchanged-copy behaviour, invalid paths, the symlink workflow
  and partial-copy failure are explicit. Notice content is defined; exact
  wording and README brevity remain human review. No new reader feature or
  permanent installation-test framework was introduced.
- Specification and design review :: The opening summary includes package
  failures and evidence. Build and copy responsibilities are separated; a
  failed copy cannot claim a successful installation. Operator-managed symlink
  testing and manual iOS transfer remain supported.
- Test-definition review :: Nine criteria and five test definitions have
  reciprocal links. The new one-off check observes artefacts and destination
  effects; source inspection covers copy-error propagation without requiring
  fault-injection infrastructure.
- Checks :: Pandoc parsing, reciprocal traceability, sanitizer comparison and
  =git diff --check= passed. No product test was executed during definition.
- Next review :: Resume the retained external context =/root/definition_audit=.

** External review - round 2
:PROPERTIES:
:ROUND: 2
:DATE: 2026-09-07
:CONTEXT: /root/definition_audit
:END:

- Revision :: SHA-256
  =9a54776bb07f54cf5163f46600d6d75640286bcb582c5a695a82a571c89e71c5=,
  committed as =6cf0b0d= (clarify reader package installation and audit evidence).
- Auditor :: Codex, OpenAI =gpt-5.6-luna=, same retained context; timeout
  8 minutes. The review returned within that limit.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions= together.
- Verdict :: PASS.
- Specification advisory :: Summary, scope, criteria, edge cases, design and
  handoff correspond. The bounded package contract does not restore retired
  feature requirements.
- Design advisory :: Explicit destination handling, the fixed three-file
  boundary, validation, unchanged copying, symlink handling and partial-copy
  warning provide a context-independent design. Non-transactional copying is
  an explicit trade-off.
- Test-definition advisory :: The package criterion has a traced bounded
  check covering build hashes, manifest compatibility, destination effects,
  unchanged repeats, invalid targets, symlink handling, failed builds and
  copy-error propagation. Source inspection of the error branch is
  proportionate; no permanent fault-injection framework is needed.
- Required corrections :: None remain in this revision.
- Supersedes :: External round 1 for the revised candidate; the earlier FAIL
  remains recorded above.
- Limit :: Definition evidence only. No implementation, build, package,
  security or native reading result is established by this verdict.

** Definition disposition

- Effective gate :: PASS after two local and two external rounds, using one
  authoring context and one retained external context.
- Recording :: The specification gate marker is PASS and the work ledger
  item is REVIEW. These lifecycle-only updates do not alter audited scope.
- Operator sign-off :: PENDING; only explicit operator authority can approve
  the definition. Implementation has not started.
- Presentation :: =sdlc-preview specs/003-full-document-reader/spec.org=
  returned =Opened spec.org= with exit status 0 on 2026-09-07. Presentation
  does not establish operator approval.

* Implementation gate

- State :: Not started; this workflow defines the change only.
